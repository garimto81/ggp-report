name: Daily Camera Report

on:
  schedule:
    # 평일 오전 10시 실행 (UTC 01:00 = KST 10:00)
    - cron: '0 1 * * 1-5'
  workflow_dispatch: # 수동 실행 가능

jobs:
  generate-report:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Build project
      run: npm run build
    
    - name: Check if weekday
      id: weekday_check
      run: |
        DAY_OF_WEEK=$(date +%u)
        echo "📅 오늘은 $(date +'%Y-%m-%d %A')입니다."
        if [ $DAY_OF_WEEK -ge 1 ] && [ $DAY_OF_WEEK -le 5 ]; then
          echo "is_weekday=true" >> $GITHUB_OUTPUT
          echo "✅ 평일입니다. 보고서를 생성합니다."
        else
          echo "is_weekday=false" >> $GITHUB_OUTPUT
          echo "🚫 주말입니다. 보고서를 생성하지 않습니다."
        fi
    
    - name: Generate Camera Report
      if: steps.weekday_check.outputs.is_weekday == 'true'
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        GOOGLE_SERVICE_ACCOUNT_KEY: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_KEY }}
        GOOGLE_DOC_ID: ${{ secrets.GOOGLE_DOC_ID }}
        TZ: Asia/Seoul
      run: |
        echo "🚀 카메라 파트 일일 보고서 생성 시작..."
        echo "📅 날짜: $(date +'%Y-%m-%d %H:%M:%S')"
        npm start -- --run-once
    
    - name: Report Status
      if: always()
      run: |
        if [ "${{ job.status }}" == "success" ]; then
          echo "✅ 일일 보고서가 성공적으로 생성되었습니다!"
          echo "📄 Google Docs 확인: https://docs.google.com/document/d/${{ secrets.GOOGLE_DOC_ID }}/"
          echo "⏰ 다음 실행: 다음 평일 오전 10시"
        else
          echo "❌ 보고서 생성 실패"
          echo "🔄 1시간 후 재시도됩니다"
        fi